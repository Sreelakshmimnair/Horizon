specVersion: "v1beta"
serviceId: "horizon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "horizon-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
